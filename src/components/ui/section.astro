---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="flex flex-col gap-0.5">
  <p class="font-accent text-xs">{label}</p>
  <div class="max-w-[55ch]">
    <slot />
  </div>
</div>
