---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div class="flex flex-col gap-2">
  <p class="font-['doto'] text-xs">{label}</p>
  <div class="max-w-[85ch]">
    <slot />
  </div>
</div>
