---
import SOCIAL_LINKS from "@/content/social-links";
import LinksGrid from "@/components/ui/links-grid.astro";

interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<LinksGrid links={SOCIAL_LINKS} class={className} />
